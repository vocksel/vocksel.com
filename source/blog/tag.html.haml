---
title: Blog
pageable: true
per_page: 12
---

%h2 Articles tagged &ldquo;#{tagname}&rdquo;

%p= prev_page()

%ul
	- page_articles.each_with_index do |article, i|
		%li
			= link_to article.title, article
			%span
				= "(" + format_time("%B #{article.date.day.ordinalize}", article.date) + ")"

%p= next_page()
